(function() {
  var isFunction, isIn;

  isFunction = require('util-ex/lib/is/type/function');

  isIn = require('util-ex/lib/is/in');

  module.exports = function(headings, filter) {
    return function(heading) {
      var result;
      result = true;
      if (isFunction(filter)) {
        result = filter(heading);
      }
      if (result) {
        result = !isIn(heading, headings);
      }
      return result;
    };
  };

}).call(this);

//# sourceMappingURL=gen-heading-filter.js.map
